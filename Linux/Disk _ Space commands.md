📊 **Загальне місце на дисках**
```bash
df -h
```
- показує розмір, зайняте та вільне місце
- `-h` = human readable

📁 **Розмір папок (що саме займає місце)**
```bash
du -h --max-depth=1
```
🔹 у поточній директорії
```bash
du -h --max-depth=1 /var
```
🔹 приклад для `/var`


📦 **Топ найбільших папок**
```bash 
du -h / | sort -h | tail -20
```
⚠️ може бути повільно, краще:
```bash
sudo du -h / | sort -h | tail -20
```

🗂️ **Розмір конкретної папки**
```bash
du -sh /path/to/folder
```
Напр: 
```bash
du -sh /home
```

📄 Розмір файлів у папці
```bash
ls -1h
```

🧹 **Кеш і пакети (часто займають місце)**
Скільки кешу apt:
```bash
du -sh /var/cache/apt
```
Очистити кеш:
```bash
sudo apt clean
```

📦 **Встановлені пакети (що важке)**
Топ найбільших пакетів:
```bash
dpkg-query -Wf '${Installed-Size}\t${Package}\n' | sort -n | tail -20
```

🐳 **Docker (якщо буде)**
```bash
docker system df
```
Очистка:
```bash
docker system prune
```

🧠 **Хто їсть диск зараз**
```bash
sudo ncdu /
```
(якщо нема — встановити)
```bash
sudo apt install ncdu
```
🔥 **дуже рекомендую**

✅ **Мінімальний must-have список**
```bash
df -h
du -sh *
ls -lh
ncdu /
```


**📝 Notes
- `/var` часто росте через логи
- `/home` — користувацькі файли
- Docker і кеш apt — перше, що перевіряти



#sh14