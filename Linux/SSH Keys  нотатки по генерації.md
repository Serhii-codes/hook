## SSH Keys — нотатки по генерації

 **Що таке SSH-ключі (коротко)**
SSH-ключі — це пара файлів для безпечної автентифікації:
- **Private key** — зберігається ТІЛЬКИ у тебе (нікому не передавати)
- **Public key** — додається на сервер, GitHub, GitLab, Bitbucket тощо

Вони замінюють логін/пароль і вважаються значно безпечнішими.

## Перевірка, чи вже є SSH-ключі
У терміналі:
```bash
ls ~/.ssh
```
Якщо бачиш файли типу:
- `id_rsa` / `id_rsa.pub`
- `id_ed25519` / `id_ed25519.pub`
-ключі вже існують.

## Рекомендований алгоритм

✅ **ed25519** — сучасний, швидкий, безпечний (рекомендовано)

## Генерація нового SSH-ключа (ed25519)

```bash
ssh-keygen -t ed25519 -C "your_email@example.com"
```

## Під час генерації:

1. **Enter file in which to save the key**  
     просто натисни `Enter` (шлях за замовчуванням: `~/.ssh/id_ed25519`)
2. **Enter passphrase**  
     можна:
    - задати пароль (безпечніше)
    - або натиснути `Enter`, щоб без пароля

## Де лежать ключі

Після генерації:
- **Private key:** `~/.ssh/id_ed25519`
- **Public key:** `~/.ssh/id_ed25519.pub`
 Private key **ніколи** не копіюється і не надсилається.

## Перегляд public key

```bash
cat ~/.ssh/id_ed25519.pub
```
Скопіюй **увесь** рядок — він починається з:
```
ssh-ed25519 AAAAC3NzaC1...
```

## Додавання ключа до SSH-agent
Щоб не вводити passphrase кожного разу:
```bash
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519
```

## Додавання SSH-ключа в GitHub (коротко)
1. GitHub → **Settings**
2. **SSH and GPG keys**
3. **New SSH key**
4. Встав public key
5. Збережи
## Перевірка підключення до GitHub

```bash
ssh -T git@github.com
```

Очікуване повідомлення:

```
Hi username! You've successfully authenticated...
```

## Права доступу (важливо)

```bash
chmod 700 ~/.ssh
chmod 600 ~/.ssh/id_ed25519
chmod 644 ~/.ssh/id_ed25519.pub
```

## Типові помилки
- ❌ Копіюють private key замість public
- ❌ Неправильні права доступу
- ❌ Генерують новий ключ замість використання існуючого
- ❌ Забувають додати ключ у ssh-agent
## Коли потрібно генерувати новий ключ
- Новий компʼютер
- Компрометація ключа
- Окремий ключ для роботи / навчання

📌 **Швидка памʼятка**
- Генерація: `ssh-keygen -t ed25519`
- Public key → сервер / GitHub
- Private key → тільки локально
- Ніколи не передавати private key
  
  #sh01