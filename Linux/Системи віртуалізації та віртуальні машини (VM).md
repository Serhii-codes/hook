## 📌 1. Що таке віртуалізація (Virtualization)

**Віртуалізація** - це технологія, яка дозволяє **одному фізичному серверу виконувати роль кількох незалежних серверів**.

Замість:
- 1 сервер = 1 ОС = 1 сервіс
Отримуємо:
- 1 сервер = багато ОС = багато сервісів

**Навіщо вона потрібна
- краща утилізація CPU / RAM
- зменшення витрат на «залізо»
- ізоляція середовищ (prod / test / dev)
- швидке масштабування
- простіший backup і recovery

## 🖥️ 2. Фізичний сервер (Host)

**Host** - це реальний сервер:
- CPU
- RAM
- SSD / HDD
- мережева карта
- BIOS / UEFI
Сам по собі він **не знає про VM**, поки не встановлено гіпервізор.

## ⚙️ 3. Гіпервізор (Hypervisor)

**Гіпервізор** - це шар ПЗ, який:
- розподіляє ресурси між VM
- запускає та зупиняє VM
- ізолює VM одна від одної
- контролює доступ до hardware
### Основні функції
- планування CPU (scheduling)
- керування памʼяттю
- віртуалізація I/O (диски, мережа)
- безпека та ізоляція

## 🧱 4. Типи гіпервізорів
## 🔹 Type 1 - Bare Metal

Працює **безпосередньо на залізі**, без хостової ОС.
```markdown
Hardware
 └── Hypervisor
      ├── VM1
      ├── VM2
      └── VM3
```
**Приклади:**
- VMware ESXi
- Microsoft Hyper-V Server
- KVM
- Xen

**Плюси:**
- максимальна продуктивність
- стабільність
- використовується в дата-центрах
**Мінуси:**
- складніше адмініструвати
- не для «домашнього» користувача

## 🔹 Type 2 - Hosted
Працює **поверх звичайної ОС**.
```markdown
Hardware
 └── Host OS
      └── Hypervisor
           ├── VM
```
**Приклади:**
- VirtualBox
- VMware Workstation
- Parallels

**Плюси:**
- легкий старт
- зручно для навчання і тестів

**Мінуси:**
- менша швидкість
- залежить від Host OS

## 🧩 5. Віртуальна машина (Virtual Machine)

**VM**  - це **повністю ізольований компʼютер у вигляді файлів**.

**VM має:
- власну ОС (Linux / Windows)
- власні користувачі
- власні процеси
- власні IP (залежить від мережі)
**Але не має:
- реального CPU
- реального диска
- прямого доступу до заліза

## 🧠 6. Як VM використовує ресурси
**CPU
- фізичні ядра → vCPU
- 1 vCPU ≠ 1 core (це логічний ресурс)
- можливий **overcommit**

**RAM
- виділяється при старті VM
- може бути:
    - static
    - ballooning
    - overcommit

**Disk
- файл на host
- типи:
    - thin provisioning
    - thick provisioning

## 💾 7. Формати віртуальних дисків
| Формат     | Де використовується   |
| ---------- | --------------------- |
| VMDK       | VMware                |
| VHD / VHDX | Hyper-V               |
| QCOW2      | KVM                   |
| RAW        | Максимальна швидкість |
## 8. Мережа у віртуальних машинах
**Основні режими

**NAT
- VM має інтернет
- ззовні недоступна
**Bridged
- VM як окремий хост у мережі
- власний IP
**Host-only
- звʼязок тільки з хостом

## 🔁 9. Snapshot
**Snapshot** — збережений стан VM:
- диск
- памʼять (інколи)
- стан ОС

**Для чого:
- перед оновленням
- тестування
- швидкий rollback
⚠️ Snapshot ≠ Backup

## 💾 10. Backup віртуальних машин
**Backup**:
- повна копія VM
- зберігається окремо
- можна відновити навіть без hypervisor
**Типи:
- full
- incremental
- differential

## 🔐 11. Безпека та ізоляція
- VM ізольовані між собою
- ризик:
    - outdated hypervisor
    - слабкі паролі
    - відсутність backup

## 🆚 12. VM vs Containers
**VM
- повна ОС
- сильна ізоляція
- важчі
**Containers
- спільне ядро
- швидкі
- менше ресурсів

## ☁️ 13. VM у хмарі
Хмарні провайдери:
- AWS (EC2)
- Azure (VM)
- GCP (Compute Engine)

Основа хмари - **віртуальні машини + автоматизація**.

## 🧑‍💻 14. Що повинен знати Support Engineer
- що таке hypervisor
- як перевірити стан VM
- різниця snapshot / backup
- базова мережа
- логіка ізоляції

## 📎 15. Коротке резюме
- VM = сервер у файлі
- Hypervisor = керівник
- Віртуалізація = основа DevOps і Cloud



#sh01 