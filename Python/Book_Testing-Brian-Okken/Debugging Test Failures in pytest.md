### Що таке `pdb`
`pdb` — це **вбудований відлагоджувач Python**, який дозволяє:
- зупиняти виконання програми в момент помилки;
- перевіряти значення змінних;
- переглядати контекст коду навколо помилки;
- крокувати по виконанню коду.
У pytest `pdb` викликається опцією:
```bash
pytest --pdb
```
Після цього pytest запускає **інтерактивну сесію відлагодження** прямо в точці збою тесту.

**Корисні pytest опції для відлагодження**
Опція	                    Призначення
--tb=auto	                long
-v / --verbose	        Показує всі назви тестів (успішні й ні)
-l / --showlocals	    Показує локальні змінні у стек-трейсі
-lf / --last-failed	    Запускає лише останні невдалі тести
-x / --exitfirst	        Зупиняє тестування після першої помилки
--pdb	                        Запускає відлагоджувач у точці збою

**Приклад використання**
```bash
pytest -v --lf -l -x --pdb
```
🔹 `-v` — докладний режим  
🔹 `--lf` — запускає тільки останні невдалі тести  
🔹 `-l` — показує локальні змінні  
🔹 `-x` — зупиняє після першої помилки  
🔹 `--pdb` — входить у pdb у точці збою

**Основні команди** `pdb`
Команда	                                                   Опис
p expr або print expr	                Виводить значення виразу
pp expr	                                            Форматований (pretty print) вивід
l або list	                                        Показує контекст коду (5 рядків вище/нижче)
l 10,20	                                            Показує рядки з 10 по 20
a або args	                                    Виводить аргументи поточної функції
u або up	                                        Переміщає один рівень угору по стеку
d або down	                                    Переміщає один рівень униз по стеку
q або quit	                                    Вихід із pdb
(назва змінної)                              Показує її значення без команди p

**Приклад у контексті тесту**:

Помилка
```python
> assert isinstance(task_id, int)
E AssertionError: assert False
E + where False = isinstance(ObjectId('59783baf8204177f24cb1b68'), int)
```

Аналіз через pdb
```bash
(Pdb) p task_id
ObjectId('59783bf48204177f2a786893')

(Pdb) p new_task
Task(summary='do something', owner=None, done=False, id=None)

```
Тут видно, що `task_id` — це **ObjectId**, а не `int`.

**Корисні поради**
- Використовуй `-x`, щоб не “потонути” у великій кількості збоїв.
- Якщо треба просто подивитись значення змінних — `-l` зручніше, ніж одразу запускати pdb.
- `--lf` дозволяє не ганяти всі тести повторно, лише ті, що не пройшли.
- Вихід з pdb (`q`) зупиняє виконання pytest.

Корисні джерела
[Python docs — pdb](https://docs.python.org/3/library/pdb.html)
[pytest docs — Debugging with pdb](https://docs.pytest.org/en/stable/how-to/failures.html#using-pdb)

#sh01 
