Для розробки використовуй порт 5000 або 8000, а порт 80 залиш для продакшену і налаштовуй через веб-сервер (Nginx).
в пакеті requests вже є метод .json() можна викликати і працювати як з словником

- Flask, Django, FastAPI, Webpy — фреймворки
- Порт для розробки: 5000 або 8000
- `requests.json()` — перетворює відповідь у словник
- `pprint.pprint()` — гарний вивід словників
- `os.environ['VAR']` — отримати змінну середовища