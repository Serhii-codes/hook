### Threading
- `threading.Thread(target=func, daemon=True)` — фоновий потік
- `.start()` — запуск
- `.join()` — дочекатися завершення
### Hash
- `hashlib.sha256()` — хешування
### CSV
- Формат таблиць, робота через модуль `csv`