- `and` — обидві умови повинні бути істинними
- `or` — достатньо однієї істинної умови
- `not` — логічне заперечення
- `elif` — "else if"
- `assert` — перевірка (використовується у тестах)

Приклад:
```python
price = 25
print(price > 10 and price < 30)  
print(price > 10 or price < 30)
```